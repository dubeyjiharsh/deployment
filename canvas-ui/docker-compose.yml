version: '3.8'

services:
  canvas-ui:
    build: .
    ports:
      - "3001:3000" # Change the exposed port to avoid conflicts
    volumes:
      - /app/node_modules
    environment:
      - NODE_ENV=production